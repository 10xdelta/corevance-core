/**
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements. See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership. The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License. You may obtain a copy of the License at
 *
 * http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied. See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
description = 'Corevance Integration Tests for OAuth2'

apply plugin: 'com.bmuschko.cargo'

// Configuration for the Gradle Cargo plugin
// https://github.com/bmuschko/gradle-cargo-plugin
configurations {
    tomcat
}

apply from: 'dependencies.gradle'

// Allow external drivers to be used for the tests without packaging it
// mainly due to license incompatibilities
configurations {
    driver
}
dependencies {
    driver 'com.mysql:mysql-connector-j'
    driver 'org.mariadb.jdbc:mariadb-java-client'
    testImplementation 'org.seleniumhq.selenium:selenium-java:4.43.0'
    testImplementation 'io.github.bonigarcia:webdrivermanager:6.3.4'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.14.4'
}

cargo {
    containerId "tomcat10x"

    // looks like Cargo doesn't detect the WAR file automatically in the multi-module setup
    deployable {
        file = file("$rootDir/corevance-war/build/libs/corevance-provider.war")
        context = 'corevance-provider'
    }

    local {
        logLevel = 'low'
        outputFile = file("$buildDir/cargo/oauth2-tests-output.log")
        installer {
            installConfiguration = configurations.tomcat
            downloadDir = file("$buildDir/download")
            extractDir = file("$buildDir/tomcat-oauth2-tests")
        }
        startStopTimeout = 240000
        sharedClasspath = configurations.driver
        containerProperties {
            def jvmArgs = '--add-exports=java.naming/com.sun.jndi.ldap=ALL-UNNAMED --add-opens=java.base/java.lang=ALL-UNNAMED --add-opens=java.base/java.lang.invoke=ALL-UNNAMED --add-opens=java.base/java.io=ALL-UNNAMED --add-opens=java.base/java.security=ALL-UNNAMED --add-opens=java.base/java.util=ALL-UNNAMED --add-opens=java.management/javax.management=ALL-UNNAMED --add-opens=java.naming/javax.naming=ALL-UNNAMED -Dcorevance.security.basicauth.enabled=false -Dcorevance.security.oauth2.enabled=true -Dcorevance.security.2fa.enabled=false '
            if (project.hasProperty('dbType')) {
                if ('postgresql'.equalsIgnoreCase(dbType)) {
                    jvmArgs += '-Dspring.datasource.hikari.driverClassName=org.postgresql.Driver -Dspring.datasource.hikari.jdbcUrl=jdbc:postgresql://localhost:5432/corevance_tenants -Dspring.datasource.hikari.username=root -Dspring.datasource.hikari.password=postgres -Dcorevance.tenant.host=localhost -Dcorevance.tenant.port=5432 -Dcorevance.tenant.username=root -Dcorevance.tenant.password=postgres'
                } else if ('mysql'.equalsIgnoreCase(dbType)) {
                    jvmArgs += '-Dspring.datasource.hikari.driverClassName=com.mysql.cj.jdbc.Driver -Dspring.datasource.hikari.jdbcUrl=jdbc:mysql://localhost:3306/corevance_tenants -Dspring.datasource.hikari.username=root -Dspring.datasource.hikari.password=mysql -Dcorevance.tenant.host=localhost -Dcorevance.tenant.port=3306 -Dcorevance.tenant.username=root -Dcorevance.tenant.password=mysql'
                } else {
                    throw new GradleException('Provided dbType is not supported')
                }
            } else {
                jvmArgs += '-Dspring.datasource.hikari.driverClassName=org.mariadb.jdbc.Driver -Dspring.datasource.hikari.jdbcUrl=jdbc:mariadb://localhost:3306/corevance_tenants -Dspring.datasource.hikari.username=root -Dspring.datasource.hikari.password=mysql -Dcorevance.tenant.host=localhost -Dcorevance.tenant.port=3306 -Dcorevance.tenant.username=root -Dcorevance.tenant.password=mysql'
            }
            jvmArgs += ' -Dspring.profiles.active=test -Dcorevance.events.external.enabled=true'
            property 'cargo.start.jvmargs', jvmArgs
            property 'cargo.tomcat.connector.keystoreFile', file("$rootDir/corevance-provider/src/main/resources/keystore.jks")
            property 'cargo.tomcat.connector.keystorePass', 'openmf'
            property 'cargo.tomcat.connector.keystoreType', 'JKS'
            property 'cargo.tomcat.httpSecure', true
            property 'cargo.tomcat.connector.sslProtocol', 'TLS'
            property 'cargo.tomcat.connector.clientAuth', false
            property 'cargo.protocol', 'https'
            property 'cargo.servlet.port', 8443
        }
    }
}

cargoRunLocal.dependsOn ':corevance-war:war'
cargoStartLocal.dependsOn ':corevance-war:war'

if (!project.hasProperty('cargoDisabled')) {
    cargoStartLocal.mustRunAfter testClasses
}

tasks.named('test').configure {
    if (!project.hasProperty('cargoDisabled')) {
        dependsOn cargoStartLocal
        finalizedBy cargoStopLocal
    }
}

// NOTE: Gradle suggested these dependencies
compileTestJava.dependsOn(':corevance-provider:generateGitProperties', ':corevance-provider:processResources', ':corevance-provider:resolve')
spotbugsTest.dependsOn(':corevance-provider:generateGitProperties', ':corevance-provider:processResources', ':corevance-provider:resolve')
